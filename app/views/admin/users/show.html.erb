
<%= @user.name %>
<%= @user.email %>

<%= @user.provider %>

<%= link_to "Delete", admin_destroy_user_path(@user), method: :delete, data: { confirm: "Are you sure you want to delete #{@user.name}"} %>